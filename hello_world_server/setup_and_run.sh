#!/bin/bash

# Install Erlang and Elixir
sudo apt update -y && sudo apt install erlang elixir -y

# Start the server
mix run --no-halt
#!/bin/bash

# Install Erlang and Elixir
sudo apt update -y && sudo apt install erlang elixir -y

# Start the server
mix run --no-halt
#!/bin/bash

# Install Erlang and Elixir
sudo apt update -y && sudo apt install erlang elixir -y

# Start the server
mix run --no-halt
#!/bin/bash

# Install Erlang and Elixir
sudo apt update -y && sudo apt install erlang elixir -y

# Start the server
mix run --no-halt
#!/bin/bash

# Install Erlang and Elixir
sudo apt update -y && sudo apt install erlang elixir -y

# Start the server
mix run --no-halt
#!/bin/bash

# Update and install Erlang and Elixir
sudo apt update -y && sudo apt install erlang elixir -y

# Start the server
mix run --no-halt






