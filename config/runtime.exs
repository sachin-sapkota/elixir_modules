import Config

# In this file, we load runtime configuration that is specific to the environment.
# This is useful for loading secrets or other environment-specific values.

# Example:
# config :hello_server, HelloServerWeb.Endpoint,
#   secret_key_base: System.get_env("SECRET_KEY_BASE")

# You can also use this file to configure other applications at runtime.
# For example, to configure the database URL:
# config :hello_server, HelloServer.Repo,
#   url: System.get_env("DATABASE_URL")
